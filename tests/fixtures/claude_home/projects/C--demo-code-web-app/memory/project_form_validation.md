---
name: Form validation rules
description: Client validation mirrors the server, and never replaces it
type: project
---

Client-side validation exists to give fast feedback. The server validates again,
always, because the client is not a trust boundary.

## Error copy
Errors name the field and say what to do, never just invalid.
